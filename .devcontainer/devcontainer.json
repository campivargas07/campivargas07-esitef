{
  "name": "ESITEF",
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "secrets": {
    "SFTP_USER": {
      "description": "SiteGround SSH username (Site Tools → Devs → SSH Keys Manager → SSH Credentials)"
    },
    "SSH_KEY_PASSPHRASE": {
      "description": "Passphrase de la clave privada en deploy/.ssh/siteground_esitef (nunca en .env.deploy)"
    }
  },
  "postStartCommand": "bash esitef-minimal/deploy/prepare-env.sh 2>/dev/null || true"
}
